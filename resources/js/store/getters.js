let getters = {
    files: state => {
        return state.files
    },
    error: state => {
        return state.error
    },
    activities: state => {
        return state.activities
    }
};

export default getters
